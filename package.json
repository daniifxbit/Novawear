{
  "name": "novawear",
  "private": true,
  "version": "1.0.0",
  "description": "NOVAWEAR — boutique streetwear, paiement par virement avec séquestre",
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c magenta,cyan \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "vercel-build": "npm run build --workspace server && npm run migrate && npm run build --workspace web",
    "migrate": "npm run migrate --workspace server",
    "start": "npm run start --workspace server",
    "seed": "npm run seed --workspace server",
    "seed:reset": "npm run seed:reset --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20.11"
  }
}
